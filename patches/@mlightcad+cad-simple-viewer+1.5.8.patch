diff --git a/node_modules/@mlightcad/cad-simple-viewer/dist/cad-simple-viewer.js b/node_modules/@mlightcad/cad-simple-viewer/dist/cad-simple-viewer.js
index 296801d..d2e3537 100644
--- a/node_modules/@mlightcad/cad-simple-viewer/dist/cad-simple-viewer.js
+++ b/node_modules/@mlightcad/cad-simple-viewer/dist/cad-simple-viewer.js
@@ -37203,8 +37203,21 @@ class mt {
   }
   static shouldRematerializeMaterial(e, t, n) {
     const i = gt(e);
-    if (t.color.isForeground)
-      return i.isForeground !== !0;
+    if (t.color.isForeground) {
+      if (i.isForeground === !0)
+        return !1;
+      if (i.isByLayerColor === !0)
+        return !0;
+      if (i.isByLayerColor == null && i.isForeground == null && i.materialKey == null) {
+        const a = Ml.fromBackgroundColor(n.currentBackgroundColor).resolveSubEntityTraitsRgb({
+          ...wi.createDefaultTraits(),
+          color: t.color,
+          layer: t.layer
+        });
+        return mt.getMaterialDisplayRgb(e) === a;
+      }
+      return !0;
+    }
     if (i.isByLayerColor === !0 && i.layer != null && i.layer !== t.layer)
       return !0;
     const s = n.currentBackgroundColor, o = Ml.fromBackgroundColor(s).resolveSubEntityTraitsRgb({
@@ -43943,20 +43956,21 @@ class _m extends Re {
    *   layer color when the entity color is BYLAYER.
    */
   refreshTextMaterials(e) {
-    if (this._colorSettings = mt.buildColorSettingsFromTraits(
+    const t = e != null && e.color && this._entityTraits.color.isByLayer ? {
+      color: e.color,
+      layer: this._entityTraits.layer
+    } : this._entityTraits;
+    this._colorSettings = mt.buildColorSettingsFromTraits(
       {
         ...wi.createDefaultTraits(),
-        color: this._entityTraits.color,
-        layer: this._entityTraits.layer
+        color: t.color,
+        layer: t.layer
       },
       this.renderContext.styleManager.currentBackgroundColor
-    ), e != null && e.color && this._entityTraits.color.isByLayer) {
-      const t = e.color.RGB;
-      typeof t == "number" && (this._colorSettings.byLayerColor = t);
-    }
+    );
     mt.rematerializeTextHierarchy(
       this,
-      this._entityTraits,
+      t,
       this.renderContext.styleManager
     );
   }
@@ -44047,12 +44061,22 @@ class _m extends Re {
    */
   attachRendered(e) {
     this.add(e);
-    const t = O_(e);
+    const t = O_(e), n = this.renderContext.database?.tables.layerTable.getAt(
+      this._entityTraits.layer
+    ), i = this._entityTraits.color.isForeground ? this._entityTraits : this._entityTraits.color.isByLayer && (n == null ? void 0 : n.color.isForeground) ? {
+      color: n.color,
+      layer: this._entityTraits.layer
+    } : void 0;
+    i && mt.rematerializeTextHierarchy(
+      t,
+      i,
+      this.renderContext.styleManager
+    );
     this.resolveDrawMode() === "unbatch" ? (this.markDrawableUnbatched(t), mt.storeTextEntityTraitsOnDrawable(
       t,
       this._entityTraits
-    )) : this.flatten(), this.removeInvalidGeometryLeaves(), this.traverse((n) => {
-      Be(n).bboxIntersectionCheck = !0;
+    )) : this.flatten(), this.removeInvalidGeometryLeaves(), this.traverse((s) => {
+      Be(s).bboxIntersectionCheck = !0;
     }), this.updateSelectionBox(e);
   }
   /**
@@ -72141,7 +72165,8 @@ class E {
    * - Dark background: use white.
    */
   resolveAci7ForBackground() {
-    return this.curView.backgroundColor === 16777215 ? 0 : 16777215;
+    const e = this.curView.backgroundColor, t = e >> 16 & 255, n = e >> 8 & 255, i = e & 255;
+    return 0.2126 * t + 0.7152 * n + 0.0722 * i >= 128 ? 0 : 16777215;
   }
   /**
    * Gets the list of available fonts that can be loaded.
@@ -72242,13 +72267,17 @@ class E {
    * ```
    */
   async openDocument(e, t, n) {
-    n = this.setOptions(n), this.onBeforeOpenDocument(n);
-    const i = await this.context.doc.openDocument(
+    n = this.setOptions(n);
+    const i = new tP(), s = await ey(i.database, () => i.openDocument(
       e,
       t,
       n
-    );
-    return this.onAfterOpenDocument(i, n), i;
+    ));
+    if (!s)
+      return i.destroy(), !1;
+    this.onBeforeOpenDocument(n);
+    const o = this.context.doc;
+    return this.context.replaceDocument(i), An().workingDatabase = i.database, Array.from(i.database.tables.layerTable.newIterator()).forEach((a) => this.curView.addLayer(a)), Array.from(i.database.objects.layout.newIterator()).forEach((a) => this.curView.addLayout(a)), o.destroy(), this.onAfterOpenDocument(!0, n), this.curView.loadLayoutEntitiesIfNeeded(this.curView.activeLayoutBtrId), !0;
   }
   /**
    * Loads a DWG/DXF file as a read-only overlay (base drawing/reference)
@@ -74934,42 +74963,56 @@ class NP {
    * @param doc - The document containing the drawing database
    */
   constructor(e, t) {
-    this._view = e, this._doc = t, t.database.events.entityAppended.addEventListener((n) => {
-      const i = uf(n.entity).filter(
-        (s) => !this.view.hasEntity(s.objectId)
-      );
-      i.length !== 0 && this.view.addEntity(i.length === 1 ? i[0] : i);
-    }), t.database.events.entityModified.addEventListener((n) => {
-      const i = n, s = this.view;
-      s instanceof fy && RP(
-        i,
-        (o) => s.hasEntity(o),
-        (o) => s.getEntityVisible(o)
-      ) && s.updateEntityVisibility(i.entity) || this.view.updateEntity(i.entity);
-    }), t.database.events.entityErased.addEventListener((n) => {
-      const i = uf(n.entity).filter(
-        (s) => this.view.hasEntity(s.objectId)
-      );
-      i.length !== 0 && this.view.removeEntity(i.length === 1 ? i[0] : i);
-    }), t.database.events.layerAppended.addEventListener((n) => {
-      this._view.addLayer(n.layer);
-    }), t.database.events.layerModified.addEventListener((n) => {
-      this._view.updateLayer(n.layer, n.changes);
-    }), Ce.instance().events.sysVarChanged.addEventListener((n) => {
+    this._view = e, this.bindDocument(t), Ce.instance().events.sysVarChanged.addEventListener((n) => {
+      if (n.database !== this._doc.database)
+        return;
       if (n.name == ce.PDMODE.toLowerCase())
         this._view.rerenderPoints(n.database.pdmode);
       else if (n.name == ce.LWDISPLAY.toLowerCase()) {
         const i = this._view, s = !!n.database.lwdisplay;
         i.renderer.showLineWeight !== s && (i.renderer.showLineWeight = s, i.clear(), n.database.regen());
       }
-    }), t.database.events.dictObjetSet.addEventListener((n) => {
-      n.object instanceof bf && this._view.addLayout(n.object);
     }), e.selectionSet.events.selectionAdded.addEventListener((n) => {
       e.highlight(n.ids);
     }), e.selectionSet.events.selectionRemoved.addEventListener((n) => {
       e.unhighlight(n.ids);
     });
   }
+  /**
+   * Rebinds this context to a fully opened replacement document.
+   *
+   * The view and command context retain their identity while subsequent
+   * database notifications come from the replacement document.
+   */
+  replaceDocument(e) {
+    this.bindDocument(e);
+  }
+  bindDocument(e) {
+    this._doc = e, e.database.events.entityAppended.addEventListener((t) => {
+      const n = uf(t.entity).filter(
+        (i) => !this.view.hasEntity(i.objectId)
+      );
+      n.length !== 0 && this.view.addEntity(n.length === 1 ? n[0] : n);
+    }), e.database.events.entityModified.addEventListener((t) => {
+      const n = t, i = this.view;
+      i instanceof fy && RP(
+        n,
+        (s) => i.hasEntity(s),
+        (s) => i.getEntityVisible(s)
+      ) && i.updateEntityVisibility(n.entity) || this.view.updateEntity(n.entity);
+    }), e.database.events.entityErased.addEventListener((t) => {
+      const n = uf(t.entity).filter(
+        (i) => this.view.hasEntity(i.objectId)
+      );
+      n.length !== 0 && this.view.removeEntity(n.length === 1 ? n[0] : n);
+    }), e.database.events.layerAppended.addEventListener((t) => {
+      this._view.addLayer(t.layer);
+    }), e.database.events.layerModified.addEventListener((t) => {
+      this._view.updateLayer(t.layer, t.changes);
+    }), e.database.events.dictObjetSet.addEventListener((t) => {
+      t.object instanceof bf && this._view.addLayout(t.object);
+    });
+  }
   /**
    * Gets the view component that renders the CAD drawing.
    *
diff --git a/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApContext.d.ts b/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApContext.d.ts
index a79f26a..2686e31 100644
--- a/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApContext.d.ts
+++ b/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApContext.d.ts
@@ -41,6 +41,14 @@ export declare class AcApContext {
      * @param doc - The document containing the drawing database
      */
     constructor(view: AcEdBaseView, doc: AcApDocument);
+    /**
+     * Rebinds this context to a fully opened replacement document.
+     *
+     * The view and command context retain their identity while subsequent
+     * database notifications come from the replacement document.
+     */
+    replaceDocument(doc: AcApDocument): void;
+    private bindDocument;
     /**
      * Gets the view component that renders the CAD drawing.
      *
diff --git a/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApContext.js b/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApContext.js
index 1ceeab4..2679aeb 100644
--- a/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApContext.js
+++ b/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApContext.js
@@ -43,6 +43,50 @@ var AcApContext = /** @class */ (function () {
     function AcApContext(view, doc) {
         var _this = this;
         this._view = view;
+        this.bindDocument(doc);
+        // Set point display mode
+        AcDbSysVarManager.instance().events.sysVarChanged.addEventListener(function (args) {
+            // Ignore sysvar notifications from detached databases (for example,
+            // a document being parsed transactionally before it becomes active).
+            if (args.database !== _this._doc.database) {
+                return;
+            }
+            if (args.name == AcDbSystemVariables.PDMODE.toLowerCase()) {
+                ;
+                _this._view.rerenderPoints(args.database.pdmode);
+            }
+            else if (args.name == AcDbSystemVariables.LWDISPLAY.toLowerCase()) {
+                var view_1 = _this._view;
+                var showLineWeight = !!args.database.lwdisplay;
+                if (view_1.renderer.showLineWeight !== showLineWeight) {
+                    view_1.renderer.showLineWeight = showLineWeight;
+                    // Existing line objects may need different geometry/material classes.
+                    // Regenerate to rebuild scene content using the new display mode.
+                    view_1.clear();
+                    args.database.regen();
+                }
+            }
+        });
+        // Show their grip points when entities are selected
+        view.selectionSet.events.selectionAdded.addEventListener(function (args) {
+            view.highlight(args.ids);
+        });
+        // Hide their grip points when entities are deselected
+        view.selectionSet.events.selectionRemoved.addEventListener(function (args) {
+            view.unhighlight(args.ids);
+        });
+    }
+    /**
+     * Rebinds this context to a fully opened replacement document.
+     *
+     * The view and command context retain their identity while subsequent
+     * database notifications come from the replacement document.
+     */
+    AcApContext.prototype.replaceDocument = function (doc) {
+        this.bindDocument(doc);
+    };
+    AcApContext.prototype.bindDocument = function (doc) {
+        var _this = this;
         this._doc = doc;
         // Add entity to scene
         doc.database.events.entityAppended.addEventListener(function (args) {
@@ -81,38 +125,12 @@ var AcApContext = /** @class */ (function () {
         doc.database.events.layerModified.addEventListener(function (args) {
             _this._view.updateLayer(args.layer, args.changes);
         });
-        // Set point display mode
-        AcDbSysVarManager.instance().events.sysVarChanged.addEventListener(function (args) {
-            if (args.name == AcDbSystemVariables.PDMODE.toLowerCase()) {
-                ;
-                _this._view.rerenderPoints(args.database.pdmode);
-            }
-            else if (args.name == AcDbSystemVariables.LWDISPLAY.toLowerCase()) {
-                var view_1 = _this._view;
-                var showLineWeight = !!args.database.lwdisplay;
-                if (view_1.renderer.showLineWeight !== showLineWeight) {
-                    view_1.renderer.showLineWeight = showLineWeight;
-                    // Existing line objects may need different geometry/material classes.
-                    // Regenerate to rebuild scene content using the new display mode.
-                    view_1.clear();
-                    args.database.regen();
-                }
-            }
-        });
         doc.database.events.dictObjetSet.addEventListener(function (args) {
             if (args.object instanceof AcDbLayout) {
                 _this._view.addLayout(args.object);
             }
         });
-        // Show their grip points when entities are selected
-        view.selectionSet.events.selectionAdded.addEventListener(function (args) {
-            view.highlight(args.ids);
-        });
-        // Hide their grip points when entities are deselected
-        view.selectionSet.events.selectionRemoved.addEventListener(function (args) {
-            view.unhighlight(args.ids);
-        });
-    }
+    };
     Object.defineProperty(AcApContext.prototype, "view", {
         /**
          * Gets the view component that renders the CAD drawing.
diff --git a/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApDocManager.js b/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApDocManager.js
index 48e9113..59c1fc9 100644
--- a/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApDocManager.js
+++ b/node_modules/@mlightcad/cad-simple-viewer/lib/app/AcApDocManager.js
@@ -549,7 +549,11 @@ var AcApDocManager = /** @class */ (function () {
      */
     AcApDocManager.prototype.resolveAci7ForBackground = function () {
         var bg = this.curView.backgroundColor;
-        return bg === 0xffffff ? 0x000000 : 0xffffff;
+        var red = (bg >> 16) & 0xff;
+        var green = (bg >> 8) & 0xff;
+        var blue = bg & 0xff;
+        var luminance = 0.2126 * red + 0.7152 * green + 0.0722 * blue;
+        return luminance >= 128 ? 0x000000 : 0xffffff;
     };
     Object.defineProperty(AcApDocManager.prototype, "avaiableFonts", {
         /**
@@ -692,17 +696,36 @@ var AcApDocManager = /** @class */ (function () {
      */
     AcApDocManager.prototype.openDocument = function (fileName, content, options) {
         return __awaiter(this, void 0, void 0, function () {
-            var isSuccess;
+            var nextDocument, isSuccess, previousDocument, currentView;
             return __generator(this, function (_a) {
                 switch (_a.label) {
                     case 0:
                         options = this.setOptions(options);
-                        this.onBeforeOpenDocument(options);
-                        return [4 /*yield*/, this.context.doc.openDocument(fileName, content, options)];
+                        nextDocument = new AcApDocument();
+                        return [4 /*yield*/, acapWithSecondaryDatabase(nextDocument.database, function () {
+                                return nextDocument.openDocument(fileName, content, options);
+                            })];
                     case 1:
                         isSuccess = _a.sent();
-                        this.onAfterOpenDocument(isSuccess, options);
-                        return [2 /*return*/, isSuccess];
+                        if (!isSuccess) {
+                            nextDocument.destroy();
+                            return [2 /*return*/, false];
+                        }
+                        this.onBeforeOpenDocument(options);
+                        previousDocument = this.context.doc;
+                        this.context.replaceDocument(nextDocument);
+                        acdbHostApplicationServices().workingDatabase = nextDocument.database;
+                        currentView = this.curView;
+                        Array.from(nextDocument.database.tables.layerTable.newIterator()).forEach(function (layer) {
+                            return currentView.addLayer(layer);
+                        });
+                        Array.from(nextDocument.database.objects.layout.newIterator()).forEach(function (layout) {
+                            return currentView.addLayout(layout);
+                        });
+                        previousDocument.destroy();
+                        this.onAfterOpenDocument(true, options);
+                        this.curView.loadLayoutEntitiesIfNeeded(this.curView.activeLayoutBtrId);
+                        return [2 /*return*/, true];
                 }
             });
         });
